{
  "name": "discord-message-transcript-monorepo",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "rimraf": "^6.1.3",
    "tsc-alias": "^1.9.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}